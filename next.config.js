/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['www.w3schools.com'] // allow images from these domain for testing
  }
}

module.exports = nextConfig
